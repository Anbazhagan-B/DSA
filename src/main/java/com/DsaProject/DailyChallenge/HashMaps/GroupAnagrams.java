package com.DsaProject.DailyChallenge.HashMaps;

import java.util.List;

/*
 * Problem: Group Anagrams
 *
 * Given an array of strings strs, group the anagrams together. You can return
 * the answer in any order.
 *
 * An anagram is a word or phrase formed by rearranging the letters of a
 * different word or phrase, typically using all the original letters exactly
 * once.
 *
 * Constraints:
 *   - 1 <= strs.length <= 10^4
 *   - 0 <= strs[i].length <= 100
 *   - strs[i] consists of lowercase English letters only.
 *
 * Examples:
 *   1) Input:  strs = ["eat", "tea", "tan", "ate", "nat", "bat"]
 *      Output: [["bat"], ["nat", "tan"], ["ate", "eat", "tea"]]
 *      Explanation: "eat", "tea", "ate" share the same letters; "tan", "nat"
 *                   share the same letters; "bat" stands alone. (Order of the
 *                   groups and of words within a group does not matter.)
 *
 *   2) Input:  strs = [""]
 *      Output: [[""]]
 *      Explanation: A single empty string forms one group on its own.
 *
 *   3) Input:  strs = ["a"]
 *      Output: [["a"]]
 *      Explanation: A single one-character string forms one group on its own.
 */
public class GroupAnagrams {

    public List<List<String>> groupAnagrams(String[] strs) {
        // TODO: implement
        return null;
    }
}
