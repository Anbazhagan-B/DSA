package com.DsaProject.DailyChallenge.Recursion;

import java.util.List;

/*
 * Problem: Generate Parentheses
 *
 * Given n pairs of parentheses, return all combinations of well-formed
 * (valid) parentheses. A combination is well-formed if every opening
 * bracket '(' has a matching closing bracket ')' and at no point does a
 * closing bracket appear without a preceding unmatched opening bracket.
 *
 * Return the combinations in any order.
 *
 * Constraints:
 *   - 1 <= n <= 8
 *
 * Examples:
 *
 *   Example 1:
 *     Input:  n = 1
 *     Output: ["()"]
 *     Explanation: With a single pair, the only valid arrangement is "()".
 *
 *   Example 2:
 *     Input:  n = 2
 *     Output: ["(())", "()()"]
 *     Explanation: Two pairs can be nested ("(())") or placed side by side ("()()").
 *
 *   Example 3:
 *     Input:  n = 3
 *     Output: ["((()))", "(()())", "(())()", "()(())", "()()()"]
 *     Explanation: All five well-formed strings using exactly three pairs of parentheses.
 */
public class GenerateParentheses {

    public List<String> generateParenthesis(int n) {
        // TODO: implement
        return null;
    }
}
