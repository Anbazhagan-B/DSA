package com.DsaProject.DailyChallenge.StacksQueues;

/*
 * Problem: Daily Temperatures
 *
 * Given an array of integers `temperatures` representing the daily temperatures,
 * return an array `answer` such that answer[i] is the number of days you have to
 * wait after the i-th day to get a warmer temperature. If there is no future day
 * for which this is possible, keep answer[i] == 0.
 *
 * Constraints:
 *   - 1 <= temperatures.length <= 10^5
 *   - 30 <= temperatures[i] <= 100
 *
 * Examples:
 *   1) Input:  [73, 74, 75, 71, 69, 72, 76, 73]
 *      Output: [1, 1, 4, 2, 1, 1, 0, 0]
 *      Explanation: Day 0 (73) waits 1 day for 74; day 2 (75) waits 4 days for 76;
 *                   day 6 (76) never sees a warmer day, so 0.
 *
 *   2) Input:  [30, 40, 50, 60]
 *      Output: [1, 1, 1, 0]
 *      Explanation: Each day is followed immediately by a warmer one; the last has none.
 *
 *   3) Input:  [30, 60, 90]
 *      Output: [1, 1, 0]
 *      Explanation: 30 -> 60 (1 day), 60 -> 90 (1 day), 90 has no warmer day ahead.
 *
 * Hint: A monotonic (decreasing) stack of indices solves this in O(n) time.
 */
public class DailyTemperatures {

    public int[] dailyTemperatures(int[] temperatures) {
        // TODO: implement
        return null;
    }
}
package com.DsaProject.DailyChallenge.StacksQueues;

/*
 * Problem: Daily Temperatures
 *
 * Given an array of integers `temperatures` representing the daily temperatures,
 * return an array `answer` such that answer[i] is the number of days you have to
 * wait after the i-th day to get a warmer temperature. If there is no future day
 * for which this is possible, keep answer[i] == 0.
 *
 * Constraints:
 *   - 1 <= temperatures.length <= 10^5
 *   - 30 <= temperatures[i] <= 100
 *
 * Examples:
 *   1) Input:  [73, 74, 75, 71, 69, 72, 76, 73]
 *      Output: [1, 1, 4, 2, 1, 1, 0, 0]
 *      Explanation: Day 0 (73) waits 1 day for 74; day 2 (75) waits 4 days for 76;
 *                   day 6 (76) never sees a warmer day, so 0.
 *
 *   2) Input:  [30, 40, 50, 60]
 *      Output: [1, 1, 1, 0]
 *      Explanation: Each day is followed immediately by a warmer one; the last has none.
 *
 *   3) Input:  [30, 60, 90]
 *      Output: [1, 1, 0]
 *      Explanation: 30 -> 60 (1 day), 60 -> 90 (1 day), 90 has no warmer day ahead.
 *
 * Hint: A monotonic (decreasing) stack of indices solves this in O(n) time.
 */
public class DailyTemperatures {

    public int[] dailyTemperatures(int[] temperatures) {
        // TODO: implement
        return null;
    }
}
package com.DsaProject.DailyChallenge.StacksQueues;

/*
 * Problem: Daily Temperatures
 *
 * Given an array of integers `temperatures` representing the daily temperatures,
 * return an array `answer` such that answer[i] is the number of days you have to
 * wait after the i-th day to get a warmer temperature. If there is no future day
 * for which this is possible, keep answer[i] == 0.
 *
 * Constraints:
 *   - 1 <= temperatures.length <= 10^5
 *   - 30 <= temperatures[i] <= 100
 *
 * Examples:
 *   1) Input:  [73, 74, 75, 71, 69, 72, 76, 73]
 *      Output: [1, 1, 4, 2, 1, 1, 0, 0]
 *      Explanation: Day 0 (73) waits 1 day for 74; day 2 (75) waits 4 days for 76;
 *                   day 6 (76) never sees a warmer day, so 0.
 *
 *   2) Input:  [30, 40, 50, 60]
 *      Output: [1, 1, 1, 0]
 *      Explanation: Each day is followed immediately by a warmer one; the last has none.
 *
 *   3) Input:  [30, 60, 90]
 *      Output: [1, 1, 0]
 *      Explanation: 30 -> 60 (1 day), 60 -> 90 (1 day), 90 has no warmer day ahead.
 *
 * Hint: A monotonic (decreasing) stack of indices solves this in O(n) time.
 */
public class DailyTemperatures {

    public int[] dailyTemperatures(int[] temperatures) {
        // TODO: implement
        return null;
    }
}
