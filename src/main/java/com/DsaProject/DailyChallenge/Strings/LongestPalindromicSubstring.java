package com.DsaProject.DailyChallenge.Strings;

/*
 * Problem: Longest Palindromic Substring
 *
 * Given a string s, return the longest substring of s that is a palindrome.
 * A palindrome reads the same forwards and backwards. A substring is a
 * contiguous sequence of characters within the string. If multiple
 * palindromic substrings share the maximum length, returning any one of
 * them is acceptable.
 *
 * Constraints:
 *   - 1 <= s.length <= 1000
 *   - s consists only of digits and English letters (upper and lower case).
 *   - Comparison is case-sensitive ('A' and 'a' are different characters).
 *
 * Examples:
 *   1) Input:  s = "babad"
 *      Output: "bab"
 *      Explanation: "bab" is a palindrome of length 3. ("aba" is also a
 *                   valid answer of the same length.)
 *
 *   2) Input:  s = "cbbd"
 *      Output: "bb"
 *      Explanation: "bb" is the longest palindromic substring (length 2);
 *                   no length-3 palindrome exists here.
 *
 *   3) Input:  s = "a"
 *      Output: "a"
 *      Explanation: A single character is always a palindrome, so the whole
 *                   string is returned.
 */
public class LongestPalindromicSubstring {

    public String longestPalindrome(String s) {
        // TODO: implement
        return null;
    }
}
